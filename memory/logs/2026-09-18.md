# 2026-09-18

## Google News reading-first integration: not yet released

- Continued the user's faithful signed-out reading-first scope. v0.8.0 remains
  the verified public desktop release; this work is not yet a new release or
  News acceptance. Playwright and interactive News remain separate unfinished
  gates. Using browser-check for component, same-input and native evidence.
- Pass-local bounded content measurement caches reduced the saved no-assets
  News render from about 15 seconds to 228 ms. The original slow pass's precise
  exhausted-limit reason was not captured. Layout fallback now records its
  first concrete node/stage when available. Existing work/depth/scene limits
  were not increased.
- Fresh production transport/resource capture: tmp/news-live.w7wMzV. Verified
  HTTPS HTTP200, 1,853,906 HTML bytes, seven sheets/1,305,432 CSS bytes, twenty
  images/57,685 bytes (seven PNG, seven JPEG, six SVG). All fourteen DOM images
  loaded. Three explicit rejected resources: foreign stylesheet, data SVG and
  oversized icon. No aggregate/request/deadline exhaustion. These are actual
  response assets, not fabricated news or evidence that JavaScript works.
- Fixed viewport-overflow propagation: an explicit HTML root, or its eligible
  first body child when the root is visible on both axes, supplies viewport
  clipping rather than clipping descendants at document y=0. Five new generic
  tests pass debug/release at 100/125/200%. Independently scrolling ordinary
  elements remain unsupported and diagnosed. Positioned ancestor clips now
  include auto/scroll as well as hidden/clip: nine positioned tests pass in
  each profile with aligned pixels, hits and extent.
- Same-capture replay tmp/news-replay.YKX2Sp: four renders at 1280x800 and
  1024x768, offsets 0/600, 250..311 ms without fallback. Scrolled stories/photos
  now remain visible. HN earlier exact checkpoint remains byte-identical at
  1024/1280, including all box/hit lists and 1,211px content height; final
  post-integration repeat is still required.
- Compared that capture in the in-app Chromium reference with scripts and
  network blocked, admitted asset URL rewrites only, and the exact regular/bold
  DejaVu fonts. tmp/news-reference.cH2GEM records all rewrites and hashes;
  originals were preserved. Reference viewports have a 15px scrollbar gutter;
  Mg's component probe does not. At 1280x800, reference document height1826
  versus Mg1838; at1024x768 both1946. This is not pixel equality or completed
  live navigation. Screens were visually inspected at the first viewport and
  offset600; remaining header/weather defects prevent acceptance.
- Actual weather image containers are empty without page hydration. The literal
  chevron_right depends on an author icon font that Mg does not load. No
  invented site-specific icons are substituted. Separately, the current weather
  is incorrectly covered by forecast content because the positioned pass does
  not preserve relative z-index ancestry and a two-stop background gradient is
  unpainted. Bounded generic corrections are being tested.
- Inline simple-shape SVG uses the existing Rust-only decoder allowlist and
  disabled resolvers. Typed CSS fill/stroke/currentColor/presentation values
  are bounded and injection-tested. New public rendering tests exposed block
  placement and preserveAspectRatio integration bugs; the initial workspace
  run failed those tests (tmp/news-workspace-checkpoint.log). Do not count that
  run green. Structured button contents also need actual child painting rather
  than a blank native-control rectangle.
- Preliminary complete archive is8,246,458 bytes, under the existing8MiB
  updater ceiling. This is a development size check labeled0.8.0, never
  published. Final version/build/package/CI/Pages/public installer and native
  release gates are still required after the visual fixes.

Next: finish the focused layout/paint fixes, repeat identical-input/reference
comparison, verify an actual served News link and Back in the native browser,
then run the full unchanged gates and publish a new feature release. Do not mark
F-025, Playwright, Google search, JSPLAN or formal MVP complete prematurely.

## Final integration and release preparation

- The earlier combined workspace run passed 1,573 tests across 87 suites, with
  zero failures and two existing manual-updater tests ignored. All normal native
  CI steps 6..13 subsequently passed, including the packaged default Boa binary,
  installation/reinstallation/checksum/desktop, updates, themes, size/bookmarks,
  smooth scrolling, inspector, tabs, stable CDP targets, workspace restart, and
  unchanged original-engine 26 native/26 external CDP journeys. Receipts:
  tmp/news-workspace-integrated.log and tmp/news-native-ci*.log. These runs
  preceded the final fit-content/anonymous-row/SVG-visibility corrections.
- Numeric stacking now uses actual paint/hit emitters, not the ancestor action
  target. The weather gradient cover and structured-button/SVG sizing fixes pass
  focused debug/release checks. All HN PNGs and complete geometry/hit logs remained
  byte-identical at the post-stacking and post-fit-content checkpoints.
- Fresh response tmp/news-live.qhKyqo: HTTP200, 1,852,460 HTML bytes, 942 nodes,
  seven CSS sheets/1,305,432 bytes and twenty assets/59,187 bytes. Its visible
  publisher wrappers require width:fit-content. The explicit typed keyword and
  bounded intrinsic sizing correction pass five public tests and adapter checks;
  tmp/news-replay.55ltWA has four no-fallback frames in 294..329ms. This does not
  conclusively identify node812 in the older, different native Back response.
- The earlier real native journey tmp/news-native-checkpoint.NSFQzu loaded home,
  followed its actual Top stories href, and used native toolbar Back, all HTTP200.
  Topic and refreshed home used readable fallback, so it was not accepted as
  faithful rendering. The manual helper now also preserves topic/back DOM and
  per-stage logs for the final rerun. No live scripts are enabled.
- Final review found missing computed display/visibility projection for inline
  SVG descendants. Added a narrow regression correction, not broader SVG support.
  Adjacent orphan table-cell header controls receive one anonymous row using the
  existing table measurement/painter; mixed noncell content remains diagnostic.
  Final checks for these slices are still running at this entry.
- Preparing v0.9.0, with F-025 still in-progress: real fonts/icons/corners,
  destination fallback and interactive News remain explicit limitations. Version,
  README, site and release notes reflect the bounded reading increment. The
  installer already tracks latest and needs public verification, not URL changes.
- Repository-package version also required the independent JSPLAN lock/audit to
  change mg-butane0.8.0 to0.9.0. No external research dependency, input, expectation
  or runtime changed. Its seven protocol tests, audit, 18 runner tests and frozen
  comparison all pass (tmp/news-final-jsplan*.log). Initial --locked failure was
  fixed by synchronizing only that package version and regenerated lock hash.
- An unpinned component-check invocation hit the known Snap wrapper restriction;
  rerunning with pinned Rust PATH across the subprocess chain passed. No guard
  or permission boundary was weakened. Release/public verification remains next.

- Final anonymous-row/SVG checkpoint: 144 related tests pass in each profile;
  the eighth anonymous-row test rejects a hidden-subtree false fallback. HN
  both PNGs and full box/hit log remain byte-identical. Original and second
  News captures render all eight size/scroll frames without fallback in
  tmp/news-replay.7pc96q4c and tmp/news-replay.q51l1l3c. Root inspected 1280 top
  and 1024 scrolled frames: header Help/Settings are now side by side, articles
  and thumbnails readable. Rounded corners, fonts and some icons still differ.
- The final native attempt tmp/news-native-final.eGUsLQ exposed a different
  actual homepage's max-width:fit-content at internal node807, class MJcqfc,
  in its topic-chip strip. This is not the previously fixed width keyword and
  not acceptance of fresh live rendering. A narrow computed-sizing correction
  is underway. The topic fallback remains explicit. Requesting its entire DOM
  exceeded the unchanged CDP4MiB response cap; the scratch driver now requests
  depth3 for topic evidence so navigation/Back can continue without raising caps.
- The complete v0.9 workspace checkpoint passed 1,597 tests in90 suites. A
  concurrent repeat subsequently hit SIGKILL in two existing Boa worker tests.
  Preserved tmp/news-final-recheck-step-3.log; no assertion/cap changes. An
  isolated nine-test run with --test-threads=1 passed all workers, including
  those two tests (tmp/news-boa-worker-repeat.log). A final ordinary/full gate
  remains required after the max-width correction. No compiler contention is
  presented as a proven root cause for the signal.
- Packaged development v0.9.0 was8,271,213 compressed bytes, below8MiB. This
  dirty-build payload is local-only; the immutable tag workflow must build and
  verify the actual public release. Current normal native gates are still running.

- Completed the native-response fit-content family after auditing every visible
  snapshot in the exact970-node captured DOM: max-width at807, overflow at811,
  min-width at813. The only remaining unrepresented snapshot is hidden node866's
  mixed calc. Flex/grid now use the same initial-offset clipping policy as ordinary
  blocks, retaining explicit unsupported element-scrolling diagnostics and Taffy's
  Scroll sizing semantics. No element scroll state/input/scrollbars were added.
  A24-case generic matrix checks pixels, boxes, hits and extent at100/125/200%.
- Independent review reproduced definite-height SVG fit sizing40x60 instead of
  120x60, plus padded64x84 instead of144x84. The fixed-height transfer correction
  now passes all four original block/flex counterexamples, with no diagnostics.
  Fourteen fit sizing and eight boundary tests pass release; an additional
  138debug checks pass. HN both images and full geometry/hit logs are unchanged.
- Exact native DOM offline diagnostic replay now has no fallback in227ms release,
  933boxes114hitsheight1766; it deliberately has no fetched assets. Receipts:
  tmp/news-native-dom-min-ratio-release.log, tmp/hn-min-ratio-release.log and
  tmp/fit-sizing-audit-fixed. Live acceptance still requires the fresh native run.
- After the earlier worker signal failure, ordinary full runs passed1603 and
  then1605tests with no relaxed assertions/caps (tmp/news-release-final*.log,
  tmp/news-static-final*.log). Full normal native gates2..13 also completed
  successfully at their documented earlier checkpoint. Final frozen candidate
  gates and packaged/native live journey are running in tmp/news-candidate-*.

- Frozen candidate package/install smoke passes at8,275,877 compressed bytes.
  The fresh live journey tmp/news-native-candidate.mk3Nzp passes: home, actual
  Top stories link, and native X11 Back all HTTP200. The driver asserts no
  readable-flow fallback for home/Back, saves DOM/metrics and actual page frames,
  and uses the link's real CDP box coordinates. Root visually inspected home,
  scroll600 and Back: readable story columns/photos, fixed header, correct weather
  clipping and restored homepage. Topic still falls back at positioned node335.
  No broad destination or full interactive News success is claimed.
- All production code is frozen. The final full workspace/release checks are
  running in tmp/news-candidate-ci*.log; publication will wait for exact-commit
  GitHub Rust/JSPLAN/Pages success and public installer/native verification.

- Final frozen candidate gates pass:1,609 full workspace tests across90 suites,
  zero failures and two existing manual-updater tests ignored; full selected
  release language/AST-array/worker and expanded geometry suites pass. Formatting,
  native dependency and component guards pass. Earlier complete native/package
  CI gates and the final candidate installer/real News checks are separate receipts.
  No assertion, test denominator, worker/resource cap or old release tag changed.
  Committing v0.9.0 for exact-commit remote gates now; publication is not yet done.

- Candidate 13b37508c9793fc10fa50f6d90d9036198b8d303 pushed to main. Exact
  Pages35325684645 and JSPLAN35325684647 pass. HTTPS site bytes match index.html;
  the public first viewport visibly contains the Mg identity, v0.9 description,
  installer and release/download links. Rust35325684662 fails the pre-existing
  multi-target CDP test at its direct cancellation-message read with WouldBlock.
  The transport queues output asynchronously, but that read treated a40ms poll
  as a delivery deadline. No release tag or artifact has been published.
- A test-only correction uses the existing three-second test deadline to retry
  WouldBlock/TimedOut for ordered events and terminal closure. All event/target
  assertions remain; a timeout no longer counts as proof of a closed route.
  A delayed-delivery regression covers ordered messages and actual closure.
  No production CDP, script, layout or resource behavior changes. Validation and
  fresh exact-commit remote gates remain required before tagging.
- Independent release review found no limitation/status contradiction. Public
  acceptance will additionally assert the exact non-dirty tagged commit and
  installed Apache NOTICE bytes, beyond the existing smoke script's checks.
- The correction passes all113 Chassis tests in each profile. Full normal
  workspace now passes1,610 tests in90 suites, with zero failures and the same
  two ignored manual-updater checks. Selected release language/AST-array/worker
  and layout gates, format/dependency/component guards, site check and diff check
  pass (tmp/news-cdp-final*.log). Pushing this test-only follow-up for new remote
  gates; production code remains the frozen reading candidate.
- Exact release commit7b5fecac59c167442a330e2a4822b153073fc69b passes remote
  Rust35326867514, JSPLAN35326867508 and Pages35326867490. Rust includes the
  complete package/installer/native controls and unchanged original/Boa journeys.
  Main matches that SHA; worktree was clean. Pushed immutable tagv0.9.0 on that
  commit. Release35328880780 is building the tagged payload; public install,
  checksum, native journey and actual update receipts are still pending.

## Public v0.9.0 verification

- Release35328880780 passes from immutable tagv0.9.0 at
  7b5fecac59c167442a330e2a4822b153073fc69b. Normal public release:
  https://github.com/pierce403/mgbrowser/releases/tag/v0.9.0.
  Archive8,271,015 bytes, within8MiB; independently downloaded checksum verifies:
  7769562d7f0943ccf7beec0deea647f2a02ac45e9412998260f949f8dc33962f.
  Only executable, icons, release README and license/NOTICE/inventory are packed.
- `python3 tools/public-release-smoke.py 0.9.0` exits0. Exact advertised curl
  command installs and atomically reinstalls in tmp/public-release-install-cu7oen36.
  Website/install/icon bytes match the repo. Version0.9.0, worker/session/Boa,
  desktop/icons/licenses and public latest-release/download links pass. Additional
  exact About commit7b5fecac59c1 and installed NOTICE byte comparison pass.
  Compile identity: Fri,18Sep2026 09:22:00GMT. Installed executable SHA-256:
  5a9f9035ce0c5007a85158b9e24ee67e2a8743148e0f4f08922c27a71cc379fe.
  Receipts: tmp/news-public-install.log, tmp/news-public-assets.mdEBPd.
- Fresh public native News journey passes in tmp/news-public-native.wjEpO0:
  home/actual Top stories link/native Back all HTTP200. Home/Back have no layout
  fallback; root visually inspected home, scroll600 and Back, with actual current
  stories/photos and fixed header. Topic remains readable-flow fallback at335.
  Scripts disabled, private profile/owned Xvfb; CDP controls link/scroll and saves
  page-only frames, native X11 activates Back. No full News or topic fidelity claim.
- Real verified publicv0.8 GUI update downloads0.9, matches the public binary
  SHA, and actual Restart exits the old process and opens the fixture in0.9.
  Worker/session and no-op update pass. Ready/restarted dialogs are inspected;
  root also checked restarted About. Evidence: tmp/news-public-gui.lVYzQq,
  tmp/news-public-gui-upgrade.log. Real user install/profile remains untouched.
- Pinned unmodified Playwright1.58.2 still exits1 at initialization over both
  HTTP/WebSocket: Target.setAutoAttach and Browser.setDownloadBehavior return
  -32601. No later client step passed. Evidence: tmp/playwright-acceptance-EyB4vi,
  tmp/news-public-playwright.log. This is a confirmed open gate, not an xfail pass.
  Seven public native helpers are finishing separately before closeout.
- All seven public native helpers exit0; tmp/news-public-native.log ends with
  PUBLIC_NATIVE_ALL_PASSED. Receipts: chrome-update.LlseyC, theme-smoke.Uf8HZc,
  scale-smoke.Rp8xWa, inspector-smoke.f13OPM, tabs-smoke.UafP2k,
  workspace-cdp-smoke.dHSPIf and workspace-restart.CVO0tQ under ignored tmp/.
  Actual screenshots were inspected for light/dark/compact/scaled controls,
  restart failure/retry, Elements/JS/resource diagnostics, stale selection,
  retained tab edits, split/detached target identity and three-tab restart.
  Public executable SHA remains unchanged. These authored fixture checks are
  separate from the actual scripts-disabled News journey and failed Playwright.
- Release increment complete. F-025 remains in-progress for full reading fidelity,
  font/icon/corner gaps, dynamic content and topic fallback. F-024 stays open;
  no full Playwright, JSPLAN, Google search or formal MVP claim. The install
  script needed no code change because its latest-release URLs now deliver0.9.0.
  Following browser-check and publish-site preserved isolated user state, actual
  packaged/public execution and exact-commit publication as separate evidence.
  Pushing a documentation-only receipt; immutable tag/binary stays7b5fecac59c1.
