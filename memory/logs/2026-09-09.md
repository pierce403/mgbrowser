# Work log: 2026-09-09 (America/Los_Angeles)


## Four-component extraction and v0.2.0 preparation

- Intent: user approved Butane/Sparkle/Chassis plus mg-browser platform composition,
  then explicitly requested implementation and push to main. The earlier release
  freeze does not block this new bounded task; compatibility work stays deferred.
- Baseline: main accda5ea59f859b8bae8f3e6fd284bf0e3543c6a, clean checkout.
- Extracted original language modules/tests into mg-butane; web host, document,
  page protocol, pure font-byte parsing and flow layout/paint into mg-sparkle.
  Chassis owns existing HTTP/TLS/cookies/navigation/CDP and optional chrome.
  mg-browser retains X11/font selection and original restricted worker code.
- Host contracts preserve generation fencing, budgets, projection validation,
  process reaping and refusal when isolation is unavailable. No native engine,
  language feature, worker relaxation or external dependency upgrade was added.
- Added eval/render/headless examples; independent no-chrome form navigation and
  exact page-pixel comparison; architecture guard; workspace CI selection.
- Docs explain provisional Rust APIs, future V8/JSC and Blink/WebKit adapters,
  Tauri integration and the ThermiteOS std/platform port. No drop-in claim.
  Prepared v0.2.0 notes, site/README and unchanged latest-tracking installer.
- Applied browser-check/publish-site and curator procedures. Updated browser-check
  with explicit workspace and independent no-chrome gates; tracked with this repo.
- Local environment initially lacked Rust/Xvfb. Installed Rust 1.91.1. Apt's
  system cache could not be written; downloaded and checksum-verified Xvfb into
  scratch. Unix display sockets are unavailable here; used an authenticated local
  TCP display for native fixture checks, with host xkbcomp supplied explicitly.
- First workspace build exposed moved import/include paths, corrected before
  testing. Debug suite passed; selected release suite passed after a transient
  linker failure disappeared when its affected target was rebuilt. No source or
  dependency change was made to bypass that failure. PNG/font/provider tests were
  retained in their owning components. Final counts/checks follow below.
- Static native form, encoded Unicode query, result link and destination passed;
  Sparkle headless and native destination frames inspected. First external CDP
  attempt reached and decoded the destination screenshot, then timed out during
  the separate flattened-session check; full publication checks remain required.
- No live website compatibility attempt was made. Existing preview limitations
  and unfinished Google goal remain explicit. Release/tag/public install pending.


### Local gates before main publication

- `cargo test --locked --workspace --all-targets`: 1,235 passed, zero failures.
- Selected original release command with `--workspace`: 1,145 passed; final changed
  Chassis library/embedding tests additionally passed in release mode.
- Independent `cargo test --locked -p mg-chassis --no-default-features --test embedding`:
  all 3 passed. Eval example returned 42; render example produced 17 layout boxes
  and 2 input regions. PNG and invalid-font/provider smoke tests passed.
- Native backend guard, component guard, Cargo formatting, generated site check,
  lockfile license inventory and diff whitespace check passed. External dependency
  versions and checksums are unchanged.
- Final v0.2.0 release build/package and `tools/release-smoke.py tmp/release` passed:
  default/custom install paths, replacement while running, checksum corruption
  rejection, worker selftest, help without fonts, LICENSE/NOTICE and desktop/icons.
- Native static journey and external CDP (including flattened-session attach,
  inspect and detach) passed on retry. Full scripted native/CDP sequence remains
  running; its completion and remote publication evidence follow separately.
