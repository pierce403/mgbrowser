# Work log: 2026-09-09 (America/Los_Angeles)


## Four-component extraction and v0.2.0 preparation

- Intent: user approved Butane/Sparkle/Chassis plus mg-browser platform composition,
  then explicitly requested implementation and push to main. The earlier release
  freeze does not block this new bounded task; compatibility work stays deferred.
- Baseline: main accda5ea59f859b8bae8f3e6fd284bf0e3543c6a, clean checkout.
- Extracted original language modules/tests into mg-butane; web host, document,
  page protocol, pure font-byte parsing and flow layout/paint into mg-sparkle.
  Chassis owns existing HTTP/TLS/cookies/navigation/CDP and optional chrome.
  mg-browser retains X11/font selection and original restricted worker code.
- Host contracts preserve generation fencing, budgets, projection validation,
  process reaping and refusal when isolation is unavailable. No native engine,
  language feature, worker relaxation or external dependency upgrade was added.
- Added eval/render/headless examples; independent no-chrome form navigation and
  exact page-pixel comparison; architecture guard; workspace CI selection.
- Docs explain provisional Rust APIs, future V8/JSC and Blink/WebKit adapters,
  Tauri integration and the ThermiteOS std/platform port. No drop-in claim.
  Prepared v0.2.0 notes, site/README and unchanged latest-tracking installer.
- Applied browser-check/publish-site and curator procedures. Updated browser-check
  with explicit workspace and independent no-chrome gates; tracked with this repo.
- Local environment initially lacked Rust/Xvfb. Installed Rust 1.91.1. Apt's
  system cache could not be written; downloaded and checksum-verified Xvfb into
  scratch. Unix display sockets are unavailable here; used an authenticated local
  TCP display for native fixture checks, with host xkbcomp supplied explicitly.
- First workspace build exposed moved import/include paths, corrected before
  testing. Debug suite passed; selected release suite passed after a transient
  linker failure disappeared when its affected target was rebuilt. No source or
  dependency change was made to bypass that failure. PNG/font/provider tests were
  retained in their owning components. Final counts/checks follow below.
- Static native form, encoded Unicode query, result link and destination passed;
  Sparkle headless and native destination frames inspected. First external CDP
  attempt reached and decoded the destination screenshot, then timed out during
  the separate flattened-session check; full publication checks remain required.
- No live website compatibility attempt was made. Existing preview limitations
  and unfinished Google goal remain explicit. Release/tag/public install pending.


### Local gates before main publication

- `cargo test --locked --workspace --all-targets`: 1,235 passed, zero failures.
- Selected original release command with `--workspace`: 1,145 passed; final changed
  Chassis library/embedding tests additionally passed in release mode.
- Independent `cargo test --locked -p mg-chassis --no-default-features --test embedding`:
  all 3 passed. Eval example returned 42; render example produced 17 layout boxes
  and 2 input regions. PNG and invalid-font/provider smoke tests passed.
- Native backend guard, component guard, Cargo formatting, generated site check,
  lockfile license inventory and diff whitespace check passed. External dependency
  versions and checksums are unchanged.
- Final v0.2.0 release build/package and `tools/release-smoke.py tmp/release` passed:
  default/custom install paths, replacement while running, checksum corruption
  rejection, worker selftest, help without fonts, LICENSE/NOTICE and desktop/icons.
- Native static journey and external CDP (including flattened-session attach,
  inspect and detach) passed on retry. Full scripted native/CDP sequence remains
  running; its completion and remote publication evidence follow separately.


### Source publication and release capability blocker

- All four local native workflow steps passed: 25 static/script journeys plus
  retained events, and their 26 external CDP counterparts. Retained interaction
  required both canceled states, Unicode/proof fields, destination and zero trap
  requests. The first flattened-session timeout did not recur on the full rerun.
- Shell `git push origin main` had no HTTPS credentials. The GitHub connector
  accepted the source. Uploaded blobs were checked against local hashes (batch
  content trees created GitHub blobs, then recursive API reads verified each).
  Final GitHub tree 1b78a30e2e7f5e64e8cdc615cbbb822f812877ea exactly equals the
  tested local tree. Fast-forward main publication created a444cada85ec80e8ac0df6858b63f4f980e4c3b8;
  local b818652 is preserved on codex/local-component-extraction. No force push.
- Pages run 34352084185 passed for a444cada. Public HTTPS index and installer
  matched local bytes. Pages configuration API is outside github_fetch's allowed
  endpoints, so current certificate/enforcement API metadata was not reverified.
- Rust run 34352084178 is the exact source-commit publication gate; final status
  is recorded below. Local release archive/installer checks already passed.
- The available GitHub connector has no tag or release creation operation.
  No GH_TOKEN/GITHUB_TOKEN is configured for shell git. Browser capability docs
  expressly prohibit provider-website fallback for unavailable plugin operations;
  no GitHub browser page or publishing action was attempted. No workflow/token
  bridge was added to evade that limitation. The v0.2.0 tag/release is still absent.
- Public site and README now explicitly say source is available, binary release
  pending, installer currently v0.1.1. Release notes and package remain ready.
  To finish: use an authenticated tag-capable path after exact-commit CI/Pages
  success, push v0.2.0 at the intended green main commit, wait for the existing
  release workflow, run tools/public-release-smoke.py 0.2.0, then remove pending
  notices and record the verified artifact. Keep existing tags immutable.


### Verified source commit

- GitHub Rust job 102467572366 / run 34352084178 completed successfully for
  a444cada85ec80e8ac0df6858b63f4f980e4c3b8: formatting and both dependency guards,
  debug and release suites, allocator examples, standalone components, package
  installation, 26 native and 26 external CDP journeys, retained interaction and
  evidence upload all green. Pages run 34352084185 is green for the same commit.
- F-013 remains in-progress only for its uncompleted binary-release/public-install
  criterion. The architecture/API work is implemented and source-published.
  This closeout changes documentation/site only; no engine or platform code differs
  from the verified source commit. No release or tag was created.

## Local macOS XQuartz installation candidate

- User requested installing on macOS and preparing any necessary fixes locally;
  public fork, push and PR require explicit approval. No publication performed.
- Baseline 7cc88685e82c3e8e139d52f143bbab112a5bef1c builds on macOS arm64,
  but install.sh rejects Darwin and default font discovery fails on Linux paths.
- Added macOS Arial candidates, XQuartz help, an opt-in source-to-app installer
  and installation documentation. The Linux binary installer remains unchanged.
- Verified on macOS 26.5.2 arm64 with XQuartz 2.8.6. XQuartz required local
  administrator authentication; after installation its launch agent was loaded
  into the current GUI session, making DISPLAY available without logging out.
- Initial tests failed on the suite's Linux font default; one CDP handshake also
  failed in that run. A complete rerun with MGBROWSER_FONT pointing to system
  Arial passed 1,154 tests across 64 target summaries, with no failures. Linux
  process tests are cfg-excluded; no Linux isolation validation is claimed.
- Release source install produced a user-level mgbrowser.app. Its installed
  executable completed the static native-handler form/results/destination
  journey with the Unicode query and hidden/submit fields, exit 0. Inspected
  tmp/macos-journey/04-destination.png. This is a local fixture, not Google.
- Normal app launch started a browser; direct launcher verification loaded
  https://example.com/ with HTTP 200 and title Example Domain. CUA inspection
  of XQuartz timed out; rendered fixture inspection and process/log verification
  succeeded. Worker selftest refused macOS as expected, without weakening isolation.
- cargo fmt, shell syntax, app plist lint, dependency guard and diff whitespace
  checks passed. No engine/dependency changes, tags, releases or upstream writes.
- Next: user review and approval before public fork/push/PR; maintainers retain
  release decisions. Native Cocoa UI and macOS script isolation remain outside
  this source-install compatibility contribution.
